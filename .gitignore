node_modules
.DS_Store
.vscode

# audit run output (regenerable)
output/
out/
*.zip

# planning artifacts: track docs, never data dumps
plans/**/*.json
plans/**/*.jsonl
plans/**/*.csv

# credentials — never commit
*service-account*.json
*credentials*.json

# local-only project instructions (never been tracked)
CLAUDE.md
